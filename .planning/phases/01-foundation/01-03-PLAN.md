---
phase: "01-foundation"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - MW-02

must_haves:
  truths:
    - "Admin routes only accessible to admin role"
    - "Student routes only accessible to student role"
    - "Routes grouped by role with proper middleware"
  artifacts:
    - path: "routes/web.php"
      provides: "Admin and student route groups"
    - path: "app/Http/Controllers/Admin/*"
      provides: "Admin controllers"
    - path: "app/Http/Controllers/Student/*"
      provides: "Student controllers"
  key_links:
    - from: "Routes"
      to: "Middleware"
      via: "role:admin, role:student middleware"
</objective>

<objective>
Create route groups for admin and student with role-based access control.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Admin Routes</name>
  <files>routes/web.php</files>
  <action>
Define admin route group in routes/web.php:

Route::middleware(['auth', 'verified', 'role:admin'])->group(function () {
    // Dashboard
    Route::get('/admin', function() {
        return view('admin.dashboard');
    })->name('admin.dashboard');
    
    // Exam management will be added in Phase 2
    // Student management will be added in Phase 2
    // Results will be added in Phase 5
});

Apply 'role:admin' middleware to protect admin routes
  </action>
  <verify>
<automated>php artisan route:list --name=admin 2>&1 | grep -q "admin"</automated>
  </verify>
  <done>Admin routes protected with role middleware</done>
</task>

<task type="auto">
  <name>Task 2: Define Student Routes</name>
  <files>routes/web.php</files>
  <action>
Define student route group:

Route::middleware(['auth', 'verified', 'role:student', 'student.active'])->group(function () {
    // Student dashboard
    Route::get('/student', function() {
        return view('student.dashboard');
    })->name('student.dashboard');
    
    // Exam sessions will be added in Phase 3
});

Apply: auth, verified, role:student, student.active middleware
  </action>
  <verify>
<automated>php artisan route:list --name=student 2>&1 | grep -q "student"</automated>
  </verify>
  <done>Student routes protected with role and active middleware</done>
</task>

<task type="auto">
  <name>Task 3: Create Route Guard for Unauthenticated</name>
  <files>routes/web.php</files>
  <action>
Ensure public routes (login, register, password reset) remain accessible:
- / (home/welcome)
- login routes (handled by Fortify)
- register routes (handled by Fortify)
- forgot-password routes (handled by Fortify)

These should be OUTSIDE any auth middleware group
  </action>
  <verify>
<automated>php artisan route:list 2>&1 | grep -qE "(login|register|home)"</automated>
  </verify>
  <done>Public routes accessible without auth</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→Routes | Role claims enter via authenticated session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Elevation | Role middleware | mitigate | Server verifies role from database, not from user input or session claims |
</threat_model>

<verification>
- [ ] php artisan route:list shows admin routes with role:admin middleware
- [ ] php artisan route:list shows student routes with role:student middleware
- [ ] Public routes accessible without auth
</verification>

<success_criteria>
Route groups defined with proper role-based access. Admin routes only accessible to admin role, student routes only accessible to student role.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/{phase}-03-SUMMARY.md`
</output>