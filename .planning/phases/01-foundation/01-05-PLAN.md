---
phase: "01-foundation"
plan: "05"
type: execute
wave: 2
depends_on:
  - "01-01-PLAN.md"
  - "01-02-PLAN.md"
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "Admin user exists in database"
    - "Network guard can be tested"
  artifacts:
    - path: "database/seeders/AdminSeeder.php"
      provides: "Creates default admin user"
  key_links:
    - from: "DatabaseSeeder"
      to: "AdminSeeder"
      via: "calls AdminSeeder"
</objective>

<objective>
Create AdminSeeder and document testing procedure for network guard.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AdminSeeder</name>
  <files>database/seeders/AdminSeeder.php</files>
  <action>
Create AdminSeeder that creates a default admin user:

- Name: "Admin"
- Email: "admin@examshield.local"
- Role: "admin"
- Is Active: true
- Password: Hash::make('admin123') or configurable via env

Add to DatabaseSeeder $seeds array after UserSeeder
  </action>
  <verify>
<automated>php artisan migrate:fresh --seed --no-interaction 2>&1 && php artisan tinker --execute "echo App\Models\User::where('role', 'admin')->first()?->email;" 2>&1 | grep -q "admin@examshield.local"</automated>
  </verify>
  <done>AdminSeeder creates admin user</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>EnforceLocalNetwork middleware</what-built>
  <how-to-verify>
1. Run: `php artisan serve --host=0.0.0.0`
2. Try accessing from external IP (simulate with curl from different IP or VPN)
3. Should see local-network.blade.php error page
4. Access from localhost/127.0.0.1 should work

For testing in development without external IP, temporarily modify middleware to log the detected IP and run: `curl -H "X-Forwarded-For: 203.0.113.1" http://localhost:8000`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed Data | Default credentials enter here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Spoofing | Default admin | mitigate | Force password change on first login or use env-configured password |
</threat_model>

<verification>
- [ ] AdminSeeder creates admin user on migrate:fresh --seed
- [ ] Network guard can be tested via curl or external IP
</verification>

<success_criteria>
Admin user created in database. Network guard tested by human or automated means.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/{phase}-05-SUMMARY.md`
</output>