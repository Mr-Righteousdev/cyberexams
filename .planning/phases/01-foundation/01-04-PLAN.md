---
phase: "01-foundation"
plan: "04"
type: execute
wave: 2
depends_on:
  - "01-03-PLAN.md"
files_modified: []
autonomous: true
requirements:
  - USER-01
  - USER-02
  - USER-03

must_haves:
  truths:
    - "Admin has navigation and admin-specific layout"
    - "Student has locked-down exam-focused layout"
  artifacts:
    - path: "resources/views/layouts/admin.blade.php"
      provides: "Admin layout with navigation"
    - path: "resources/views/layouts/student.blade.php"
      provides: "Student exam layout (locked down)"
    - path: "resources/views/errors/local-network.blade.php"
      provides: "Network blocked error page"
  key_links:
    - from: "Views"
      to: "Routes"
      via: "layout() calls in route closures/controllers"
</objective>

<objective>
Create admin and student layouts with navigation, error pages.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Admin Layout</name>
  <files>resources/views/layouts/admin.blade.php</files>
  <action>
Create admin layout with:

1. Use Tailwind CSS 4 via Laravel's asset() helper
2. Include navigation with:
   - Logo/App name
   - Navigation links: Dashboard, Exams, Students, Results
   - Admin name display with dropdown (settings, logout)
3. Main content area with @yield('content')
4. Flash messages for actions
5. Use standard Laravel blade syntax

File: resources/views/layouts/admin.blade.php
  </action>
  <verify>
<automated>ls -la resources/views/layouts/admin.blade.php</automated>
  </verify>
  <done>Admin layout created with navigation</done>
</task>

<task type="auto">
  <name>Task 2: Create Student Exam Layout</name>
  <files>resources/views/layouts/student.blade.php</files>
  <action>
Create student exam layout (locked down):

1. Minimal header with: App name, student name (no navigation - locked down)
2. Clean, distraction-free main area
3. NO sidebar
4. Warning about exam integrity
5. No external links
6. Minimal JS required for exam functionality

This layout should be used ONLY during exams to prevent cheating via UI manipulation.
  </action>
  <verify>
<automated>ls -la resources/views/layouts/student.blade.php</automated>
  </verify>
  <done>Student exam layout created (minimal, locked down)</done>
</task>

<task type="auto">
  <name>Task 3: Create Network Blocked Error Page</name>
  <files>resources/views/errors/local-network.blade.php</files>
  <action>
Create error page at resources/views/errors/local-network.blade.php:

1. Clear message explaining access is blocked
2. Reason: "This application is only accessible from local networks"
3. Contact admin message
4. Clean, minimal styling (can use guest layout)

This is rendered when EnforceLocalNetwork middleware blocks access.
  </action>
  <verify>
<automated>ls -la resources/views/errors/local-network.blade.php</automated>
  </verify>
  <done>Network blocked error page created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI→Admin | Admins should not see student exam content |

## STRIDE Threat Register

| Threat ID | Category | Disclosure | Component | Disposition |
|-----------|-----------|------------|-----------|-------------|
| T-01-06 | Disclosure | Student layout | mitigate | Minimize exposed elements in student layout |
</threat_model>

<verification>
- [ ] Admin layout exists with proper navigation
- [ ] Student layout exists (minimal)
- [ ] Network error page exists
</verification>

<success_criteria>
Admin and student layouts created. Network blocked error page created.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/{phase}-04-SUMMARY.md`
</output>