---
phase: "02-exam-builder"
plan: "03"
type: execute
wave: 3
depends_on: ["02-01-PLAN"]
files_modified:
  - "app/Livewire/Admin/StudentIndex.php"
  - "app/Livewire/Admin/StudentCreate.php"
  - "app/Livewire/Admin/StudentBulkCreate.php"
  - "app/Livewire/Admin/StudentEdit.php"
  - "app/Livewire/Admin/StudentResetPassword.php"
  - "app/Actions/Admin/ResetStudentPassword.php"
  - "resources/views/livewire/admin/student/index.blade.php"
  - "resources/views/livewire/admin/student/create.blade.php"
  - "resources/views/livewire/admin/student/bulk-create.blade.php"
  - "resources/views/livewire/admin/student/edit.blade.php"
  - "routes/admin.php"
autonomous: true
requirements:
  - STUD-01
  - STUD-02
  - STUD-03
  - STUD-04

must_haves:
  truths:
    - "Admin can create individual student accounts"
    - "Admin can bulk create multiple students from CSV/text"
    - "Admin can deactivate/reactivate student accounts"
    - "Admin can reset student passwords"
    - "Students get default password for first login"
  artifacts:
    - path: "app/Livewire/Admin/StudentIndex.php"
      provides: "Student list with status toggle"
    - path: "app/Livewire/Admin/StudentCreate.php"
      provides: "Single student creation form"
    - path: "app/Livewire/Admin/StudentBulkCreate.php"
      provides: "Bulk student creation"
    - path: "app/Livewire/Admin/StudentEdit.php"
      provides: "Student edit (name, email, student_number, status)"
    - path: "app/Livewire/Admin/StudentResetPassword.php"
      provides: "Password reset action"
    - path: "app/Actions/Admin/ResetStudentPassword.php"
      provides: "Password reset logic"
  key_links:
    - from: "app/Livewire/Admin/StudentIndex.php"
      to: "app/Models/User.php"
      via: "User::where('role', 'student')->get()"
    - from: "app/Livewire/Admin/StudentBulkCreate.php"
      to: "app/Models/User.php"
      via: "User::insert() for bulk insert"
---

<objective>
Student account management. Admins can create, bulk create, toggle active status, and reset passwords for student accounts.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-exam-builder/02-01-PLAN.md
@app/Models/User.php
</context>

<interfaces>
<!-- Key interfaces for student management -->

From app/Models/User.php:
```php
// Additional properties for students:
// - role: 'admin' | 'student' (enum)
// - student_number: string, nullable, unique
// - is_active: boolean (default true)

// Methods:
// - isAdmin(): bool
// - isStudent(): bool

// Relationships:
// - exams(): hasMany(Exam::class, 'admin_id')
// - examSessions(): hasMany(ExamSession::class)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Student Index Livewire Component</name>
  <files>app/Livewire/Admin/StudentIndex.php, resources/views/livewire/admin/student/index.blade.php</files>
  <action>
Create StudentIndex Livewire component:
- Computed property: $students with User::where('role', 'student')->orderBy('name')->get()
- Add toggleStatus(User $student) method — toggles is_active
- Add resetPassword(User $student) method — generates new temp password
- Add editStudent(User $student) method — redirect to edit
- Add deleteStudent(User $student) method — with confirmation
- Create view with table showing:
  - Name
  - Email
  - Student Number (if set)
  - Status (Active/Inactive badge)
  - Created Date
  - Actions: Edit, Reset Password, Toggle Active, Delete

Rules:
- Exclude admins from list (only role=student)
- Show active/inactive status with visual indicator (green/red badge)
- Show "No students yet" empty state
  </action>
  <verify>
<automated>php artisan route:list --name=admin.students</automated>
  </verify>
  <done>Admin can view student list and toggle status</done>
</task>

<task type="auto">
  <name>Task 2: Student Create Livewire Component</name>
  <files>app/Livewire/Admin/StudentCreate.php, resources/views/livewire/admin/student/create.blade.php</files>
  <action>
Create StudentCreate Livewire component:
- Properties: name, email, student_number (optional)
- Validation rules:
  - name: required, string, max:255
  - email: required, email, unique:users,email
  - student_number: nullable, string, unique:users,student_number, max:50
- save() method:
  - Sets role='student'
  - Sets is_active=true
  - Generates temporary password (e.g., "Student123!")
  - Sends notification with temp password (or shows on success for immediate use)
- Redirect to student index after save

Rules:
- Student number is optional per REQUIREMENTS
- Default temporary password must meet password requirements
- Set must_change_password=true (Force password change on first login)
  </action>
  <verify>
<automated>php artisan route:list --name=admin.students.create</automated>
  </verify>
  <done>Admin can create individual student account</done>
</task>

<task type="auto">
  <name>Task 3: Student Bulk Create Livewire Component</name>
  <files>app/Livewire/Admin/StudentBulkCreate.php, resources/views/livewire/admin/student/bulk-create.blade.php</files>
  <action>
Create StudentBulkCreate Livewire component:
- Accept textarea input with format options:
  - CSV: name,email,student_number (header row optional detection)
  - One per line: "John Doe,john@school.edu,STU001"
- Properties:
  - format: 'csv' | 'text' (default csv)
  - import_data (textarea)
- Validation:
  - import_data: required, string
- Process:
  - Parse each line into name, email, student_number
  - Validate email format
  - Check for duplicates (email)
  - Create users with batch insert for performance
- save() method:
  - Creates all valid students
  - Sets role='student', is_active=true
  - Generates temp password for each
  - Returns results: success count, failed rows with reasons

Import format rules:
- Skip empty lines
- Skip lines starting with # (comments)
- Support "name,email" or "name,email,student_number"
- Trim whitespace

Rules:
- Show preview of parsed data before confirming
- Display errors for invalid rows
- Show "Created X students, Y failed" message after
  </action>
  <verify>
<automated>php artisan route:list --name=admin.students.bulk</automated>
  </verify>
  <done>Admin can bulk create students from text input</done>
</task>

<task type="auto">
  <name>Task 4: Student Edit and Password Reset</name>
  <files>app/Livewire/Admin/StudentEdit.php, app/Livewire/Admin/StudentResetPassword.php, app/Actions/Admin/ResetStudentPassword.php</files>
  <action>
Create StudentEdit Livewire component:
- Accept user_id from route parameter
- Properties: name, email, student_number, is_active
- mount() populates from user
- update() saves changes

Create StudentResetPassword Livewire action:
- Accept user_id from route parameter
- Generates new temp password
- Updates password hash
- Sets must_change_password=true
- Shows new password to admin (copies to clipboard button)
- Redirects back

Create ResetStudentPassword action class:
- Generates secure random password
- Hashes and saves to user
- Sets must_change_password flag
- Returns plain text password for display
  </action>
  <verify>
<automated>php artisan route:list --name=admin.students.edit</automated>
  </verify>
  <done>Admin can edit student and reset password</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin→Student | Admin manages students — trust admin input |
| Form Input→Database | CSRF protected, validated input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | Student passwords | mitigate | Never store/show plain password except immediately after reset |
| T-02-06 | Tampering | Student is_active | mitigate | Only admin role can toggle — enforced by middleware |
</threat_model>

<verification>
- [ ] Access /admin/students — list all students
- [ ] Create single student — saved with role=student
- [ ] Bulk create with 5 valid rows — all created
- [ ] Toggle student active/inactive — status changes
- [ ] Reset password — new temp password shown
- [ ] Edit student — changes persisted
</verification>

<success_criteria>
Admin can create single and bulk students, toggle active status, edit details, and reset passwords. All actions work correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-exam-builder/{phase}-03-SUMMARY.md`
</output>