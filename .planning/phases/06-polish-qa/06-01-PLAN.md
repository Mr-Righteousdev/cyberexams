---
phase: "06-polish-qa"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/views/errors/403.blade.php
  - resources/views/errors/404.blade.php
  - resources/views/errors/419.blade.php
  - resources/views/errors/500.blade.php
  - resources/views/errors/503.blade.php
  - resources/views/components/empty-state.blade.php
  - resources/views/components/loading-state.blade.php
  - resources/views/livewire/admin/exam-index.blade.php
  - resources/views/livewire/admin/student-index.blade.php
  - resources/views/livewire/admin/exam-results.blade.php
autonomous: true
requirements: []
must_haves:
  truths:
    - "User sees a branded error page for any HTTP error (403, 404, 419, 500, 503)"
    - "User sees an empty state message instead of a blank page when no data exists"
    - "UI layouts render correctly on laptop screen sizes (1024px-1440px)"
  artifacts:
    - path: "resources/views/errors/{403,404,419,500,503}.blade.php"
      provides: "Branded error pages matching network-blocked style"
      min_lines: 25
    - path: "resources/views/components/empty-state.blade.php"
      provides: "Reusable empty state component for tables and lists"
      min_lines: 20
    - path: "resources/views/components/loading-state.blade.php"
      provides: "Reusable loading indicator component"
      min_lines: 10
  key_links:
    - from: "resources/views/livewire/admin/exam-index.blade.php"
      to: "resources/views/components/empty-state.blade.php"
      via: "@if($exams->isEmpty()) <x-empty-state> component"
      pattern: "empty-state"
    - from: "resources/views/errors/404.blade.php"
      to: "resources/views/errors/network-blocked.blade.php"
      via: "Same visual style (bg-gray-50, rounded-xl, centered)"
      pattern: "network-blocked"
---

<objective>
Create error pages for HTTP exceptions (403, 404, 419, 500, 503), a reusable empty-state component for tables and lists, and a loading-state component. Also audit responsive layouts at laptop sizes (1024px-1440px).

Purpose: All error states must show branded ExamShield pages — not generic Laravel yellow error pages. Empty states must guide users (not show blank white space).

Output: Custom error blade pages in resources/views/errors/, reusable components in resources/views/components/.
</objective>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From resources/views/errors/network-blocked.blade.php (the reference implementation — copy this style):
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>... - ExamShield</title>
    @vite(['resources/css/app.css'])
</head>
<body class="bg-gray-50">
    <div class="min-h-screen flex items-center justify-center">
        <div class="max-w-md w-full bg-white rounded-xl shadow-lg p-8 text-center">
            <!-- Icon, heading, description, action button -->
        </div>
    </div>
</body>
</html>
```

From resources/views/livewire/admin/dashboard.blade.php (Flux UI patterns):
```html
<flux:card>
    <flux:heading size="sm">Title</flux:heading>
    <flux:text size="xl" class="font-bold">{{ $value }}</flux:text>
</flux:card>
@if(empty($items))
    <flux:callout variant="warning">No items yet. Create one to get started.</flux:callout>
@endif
```

From resources/views/livewire/admin/exam-index.blade.php (if it exists — check before writing):
```php
// Current structure: @if($exams->isEmpty()) @else @foreach @endforeach @endif
// If it uses <flux:table>, the empty state should replace the table
```
</interfaces>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md — Phase 6 tasks: Error states, Empty states, Responsive UI pass
@.planning/codebase/CONVENTIONS.md — Flux UI patterns, Tailwind v4
</context>

<read_first>
- resources/views/errors/network-blocked.blade.php (reference error page style)
- resources/views/livewire/admin/dashboard.blade.php (Flux UI card patterns)
- resources/views/livewire/admin/exam-index.blade.php (check existing table structure)
- resources/views/livewire/admin/student-index.blade.php (check existing table structure)
- resources/views/livewire/admin/exam-results.blade.php (check results empty state)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Create branded HTTP error pages</name>
  <files>resources/views/errors/403.blade.php, resources/views/errors/404.blade.php, resources/views/errors/419.blade.php, resources/views/errors/500.blade.php, resources/views/errors/503.blade.php</files>
  <action>
Create custom error pages for each HTTP status code, matching the style of the existing network-blocked.blade.php (bg-gray-50 body, centered white rounded-xl card, SVG icon, heading, description, action link).

**403.blade.php:**
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Access Denied - ExamShield</title>
    @vite(['resources/css/app.css'])
</head>
<body class="bg-gray-50">
    <div class="min-h-screen flex items-center justify-center">
        <div class="max-w-md w-full bg-white rounded-xl shadow-lg p-8 text-center">
            <div class="text-red-500 mb-4">
                <svg class="w-16 h-16 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
                </svg>
            </div>
            <h1 class="text-2xl font-semibold text-gray-900 mb-2">Access Denied</h1>
            <p class="text-gray-600 mb-6">You don't have permission to access this page.</p>
            <a href="{{ url()->previous() }}" class="inline-flex items-center justify-center px-6 py-3 bg-indigo-600 text-white font-medium rounded-lg hover:bg-indigo-700">Go Back</a>
        </div>
    </div>
</body>
</html>
```

**404.blade.php:** Title "Page Not Found", SVG icon as lock/shield/X mark, description "The page you're looking for doesn't exist or has been moved."

**419.blade.php:** Title "Session Expired", description "Your session has expired. Please refresh and try again." with refresh link.

**500.blade.php:** Title "Server Error", description "Something went wrong on our end. Please try again later." with home link.

**503.blade.php:** Title "Service Unavailable", description "The exam platform is currently unavailable. Please try again in a few moments."

Use the EXACT same HTML structure as network-blocked.blade.php for consistency. Only change: title text, h1 heading, SVG icon path, p description, and link text.
  </action>
  <verify>
    <automated>
ls resources/views/errors/403.blade.php resources/views/errors/404.blade.php resources/views/errors/419.blade.php resources/views/errors/500.blade.php resources/views/errors/503.blade.php && grep -l "bg-gray-50" resources/views/errors/*.blade.php | wc -l | grep 5
    </automated>
  </verify>
  <done>All 5 error pages exist with branded ExamShield style matching network-blocked.blade.php</done>
  <acceptance_criteria>
    - resources/views/errors/403.blade.php exists and contains "Access Denied" and "bg-gray-50"
    - resources/views/errors/404.blade.php exists and contains "Page Not Found" and "bg-gray-50"
    - resources/views/errors/419.blade.php exists and contains "Session Expired" and "bg-gray-50"
    - resources/views/errors/500.blade.php exists and contains "Server Error" and "bg-gray-50"
    - resources/views/errors/503.blade.php exists and contains "Service Unavailable" and "bg-gray-50"
    - All 5 files have the same HTML structure as network-blocked.blade.php
    - All 5 files include @vite(['resources/css/app.css'])
    - All 5 files have a centered, white, rounded-xl card design
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create reusable empty-state component</name>
  <files>resources/views/components/empty-state.blade.php</files>
  <action>
Create a reusable Livewire Blade component at resources/views/components/empty-state.blade.php that can be included in any table or list view.

```html
{{-- resources/views/components/empty-state.blade.php --}}
@props([
    'title' => 'Nothing here yet',
    'description' => 'Get started by creating one.',
    'actionRoute' => null,
    'actionLabel' => 'Create New',
    'icon' => 'folder', // folder | users | clipboard | chart
])

<div class="flex flex-col items-center justify-center py-12 text-center">
    <div class="text-gray-300 mb-4">
        @if($icon === 'folder')
            <svg class="w-16 h-16 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z" />
            </svg>
        @elseif($icon === 'users')
            <svg class="w-16 h-16 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h18v-1a6 6 0 00-9-5.197m13.5-9a2.5 2.5 0 11-5 0 2.5 2.5 0 015 0z" />
            </svg>
        @elseif($icon === 'clipboard')
            <svg class="w-16 h-16 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
            </svg>
        @else
            <svg class="w-16 h-16 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
            </svg>
        @endif
    </div>
    <h3 class="text-lg font-medium text-gray-900 mb-1">{{ $title }}</h3>
    <p class="text-gray-500 mb-4">{{ $description }}</p>
    @if($actionRoute)
        <a href="{{ $actionRoute }}" class="inline-flex items-center px-4 py-2 bg-indigo-600 text-white text-sm font-medium rounded-lg hover:bg-indigo-700">
            {{ $actionLabel }}
        </a>
    @endif
</div>
```

Then update exam-index.blade.php, student-index.blade.php, and exam-results.blade.php to use this component instead of bare @if(empty()) with <flux:callout>. Replace the conditional empty state blocks with `<x-empty-state>`.
  </action>
  <verify>
    <automated>
ls resources/views/components/empty-state.blade.php && grep -l "@props" resources/views/components/empty-state.blade.php && grep -c "icon" resources/views/components/empty-state.blade.php
    </automated>
  </verify>
  <done>Reusable empty-state component created and used in exam, student, and results views</done>
  <acceptance_criteria>
    - resources/views/components/empty-state.blade.php exists with @props for title, description, actionRoute, actionLabel, icon
    - Component supports 4 icon variants (folder, users, clipboard, chart)
    - exam-index.blade.php uses <x-empty-state> instead of bare @if empty check
    - student-index.blade.php uses <x-empty-state> for empty student list
    - exam-results.blade.php uses <x-empty-state> for empty results state
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Audit and fix responsive layout issues</name>
  <files>resources/views/livewire/admin/dashboard.blade.php, resources/views/livewire/admin/exam-index.blade.php, resources/views/livewire/student/exam-start.blade.php, resources/views/livewire/student/exam-taking.blade.php</files>
  <action>
Audit all admin and student layouts/views for responsive issues at laptop screen sizes (1024px-1440px):

1. **Check grid layouts:** All `grid-cols-1 md:grid-cols-N` patterns should have proper md: breakpoints. Verify no fixed pixel widths that overflow on smaller laptops.

2. **Fix dashboard stats:** The 4-column grid on dashboard should collapse to 2 columns on medium screens (md:grid-cols-2) and stay 4 on large (lg:grid-cols-4). Currently it may be `grid-cols-1 md:grid-cols-4` which creates a 4-wide strip on laptops — change to `grid-cols-2 md:grid-cols-4`:

```html
<div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">
```

3. **Check table overflow:** <flux:table> in exam-index and student-index should not have `overflow-x-auto` causing double scroll. Add responsive table wrapping where needed.

4. **Check exam-taking layout:** The exam-taking view has a two-panel layout (question list + answer area). Ensure it doesn't overflow on 1024px width. Add `overflow-hidden` to prevent scroll bleeding.

Read each file first, identify specific issues, fix them with Tailwind utility classes. Do NOT make broad CSS changes — only targeted fixes.
  </action>
  <verify>
    <automated>
grep -c "grid-cols-2 md:grid-cols-4" resources/views/livewire/admin/dashboard.blade.php && grep -c "overflow" resources/views/livewire/admin/exam-index.blade.php resources/views/livewire/student/exam-taking.blade.php 2>/dev/null
    </automated>
  </verify>
  <done>All layouts render correctly at 1024px-1440px laptop sizes</done>
  <acceptance_criteria>
    - dashboard.blade.php uses `grid-cols-2 md:grid-cols-4` for stats cards
    - exam-taking has overflow containment to prevent scroll bleeding
    - exam-index table has responsive width handling
    - No fixed widths > 1440px in any layout file
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Run `php artisan route:list --except-vendor` to confirm all routes render correctly
2. Manually test error pages by triggering each HTTP status (404 by visiting /nonexistent-route, 403 by forcing a forbidden response)
3. Test empty states by visiting admin pages with no data (empty exams, empty students)
4. Test at laptop screen sizes: 1024px wide (sidebar may collapse), 1280px, 1440px
</verification>

<success_criteria>
- All 5 HTTP error pages (403, 404, 419, 500, 503) display branded ExamShield pages matching network-blocked style
- Empty state component is reusable across all table/list views
- Dashboard stats cards display in a balanced 2x2 grid at laptop size, 4 across at larger screens
- No layout overflow or double-scroll at 1024px-1440px laptop sizes
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-qa/06-01-SUMMARY.md`
</output>